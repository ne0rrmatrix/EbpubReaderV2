<?xml version="1.0" encoding="utf-8" ?>
<!--
    Variation 3 — "Dark Cinema"
    Always-dark, cinematic reading-room look: deep charcoal surfaces, teal accent,
    soft card strokes. Colors are hardcoded to the dark palette (no AppThemeBinding)
    so this variant reads as a fixed "cinema mode" regardless of system theme.
    Fully drop-in (GridItemsLayout + BooksLayout).
-->
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:DisplayBook.App.Models"
             xmlns:vm="clr-namespace:DisplayBook.App.ViewModels"
             xmlns:views="clr-namespace:DisplayBook.App.Views"
             x:Class="DisplayBook.App.Views.LibraryPage"
             x:Name="Page"
             x:DataType="vm:LibraryViewModel"
             Title="Library">
    <Grid RowDefinitions="Auto,Auto,Auto,*" Padding="24" RowSpacing="16"
          Background="#0E1013">

        <!-- Header + primary actions -->
        <Grid ColumnDefinitions="{OnIdiom Phone='*', Default='*,Auto'}" RowDefinitions="{OnIdiom Phone='Auto,Auto', Default='Auto'}" ColumnSpacing="10">
            <VerticalStackLayout Spacing="4">
                <Label Text="Your library"
                       FontSize="32"
                       FontAttributes="Bold"
                       TextColor="#F2F5F8"
                       SemanticProperties.HeadingLevel="Level1" />
                <Label Text="Choose a book to read"
                       TextColor="#9AA5B5"
                       FontSize="16" />
            </VerticalStackLayout>
            <HorizontalStackLayout Grid.Column="{OnIdiom Phone=0, Default=1}"
                                   Grid.Row="{OnIdiom Phone=1, Default=0}"
                                   Spacing="8"
                                   VerticalOptions="Center"
                                   IsVisible="{Binding IsNotSelectionMode}">
                <Button Text="Select"
                        Command="{Binding EnterSelectionModeCommand}"
                        Background="Transparent"
                        TextColor="#66E0C1"
                        BorderColor="#2E4A42"
                        BorderWidth="1"
                        CornerRadius="10" />
                <Button Text="Add EPUB"
                        Command="{Binding ImportFileCommand}"
                        Background="#236B5A"
                        TextColor="White"
                        CornerRadius="10" />
                <Button Text="Add folder"
                        Command="{Binding ImportFolderCommand}"
                        Background="#1C3A33"
                        TextColor="#66E0C1"
                        CornerRadius="10" />
            </HorizontalStackLayout>
            <HorizontalStackLayout Grid.Column="{OnIdiom Phone=0, Default=1}"
                                   Grid.Row="{OnIdiom Phone=1, Default=0}"
                                   Spacing="2"
                                   VerticalOptions="Center"
                                   IsVisible="{Binding IsSelectionMode}">
                <Label Text="{Binding SelectedCount, StringFormat='{0} selected'}"
                       VerticalOptions="Center"
                       TextColor="#9AA5B5" />
                <Button Text="Select all"
                        Command="{Binding SelectAllCommand}"
                        Background="Transparent"
                        TextColor="#66E0C1"
                        Padding="8,0" />
                <Button Text="Clear"
                        Command="{Binding ClearSelectionCommand}"
                        Background="Transparent"
                        TextColor="#66E0C1"
                        Padding="8,0" />
                <Button Text="Delete"
                        Command="{Binding DeleteSelectedCommand}"
                        Background="#A83A3A"
                        TextColor="White"
                        CornerRadius="10" />
                <Button Text="Cancel"
                        Command="{Binding ExitSelectionModeCommand}"
                        Background="#1C3A33"
                        TextColor="#66E0C1"
                        CornerRadius="10" />
            </HorizontalStackLayout>
        </Grid>

        <!-- Search + sort -->
        <Grid Grid.Row="1" ColumnDefinitions="{OnIdiom Phone='*', Default='*,Auto'}" RowDefinitions="{OnIdiom Phone='Auto,Auto', Default='Auto'}" ColumnSpacing="12">
            <SearchBar Placeholder="Search by title or author"
                       Text="{Binding SearchQuery, Mode=TwoWay}"
                       CancelButtonColor="#66E0C1"
                       HorizontalOptions="Fill" />
            <Picker Grid.Column="{OnIdiom Phone=0, Default=1}"
                    Grid.Row="{OnIdiom Phone=1, Default=0}"
                    Title="Sort library"
                    ItemsSource="{Binding SortOptions}"
                    SelectedItem="{Binding SortOption, Mode=TwoWay}"
                    WidthRequest="{OnIdiom Phone=-1, Default=190}"
                    HorizontalOptions="{OnIdiom Phone=Fill, Default=Center}"
                    VerticalOptions="Center" />
        </Grid>

        <Label Grid.Row="2"
               Text="{Binding ErrorMessage}"
               TextColor="#FF8A80"
               IsVisible="{Binding HasError}"
               LineBreakMode="WordWrap" />

        <!-- Dark card grid -->
        <RefreshView Grid.Row="3"
                     IsRefreshing="{Binding IsRefreshing}"
                     Command="{Binding LoadCommand}">
            <CollectionView x:Name="BooksCollection"
                            ItemsSource="{Binding VisibleBooks}"
                            SelectionMode="None"
                            ItemSizingStrategy="MeasureAllItems">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout x:Name="BooksLayout"
                                     Orientation="Vertical"
                                     Span="2"
                                     HorizontalItemSpacing="18"
                                     VerticalItemSpacing="24" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:LibraryBookItem">
                        <Border Stroke="#2A3038"
                                Background="#1A1E24"
                                StrokeThickness="1"
                                StrokeShape="RoundRectangle 14"
                                Padding="10">
                            <Border.Triggers>
                                <DataTrigger TargetType="Border"
                                             Binding="{Binding IsSelected}"
                                             Value="True">
                                    <Setter Property="Stroke" Value="#5BD6B5" />
                                    <Setter Property="Background" Value="#16302A" />
                                    <Setter Property="StrokeThickness" Value="2" />
                                </DataTrigger>
                            </Border.Triggers>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={x:Reference Page}, Path=ViewModel.OpenDetailsCommand, x:DataType=views:LibraryPage}"
                                                      CommandParameter="{Binding}" />
                            </Border.GestureRecognizers>
                            <Grid RowDefinitions="{OnIdiom Phone='300,Auto,Auto', Default='360,Auto,Auto'}" RowSpacing="9">
                                <Grid Grid.Row="0">
                                    <Border Background="#0B0D10"
                                            Stroke="#262C34"
                                            StrokeThickness="1"
                                            StrokeShape="RoundRectangle 8"
                                            Padding="0">
                                        <Image Source="{Binding CoverPath}"
                                               Aspect="AspectFill"
                                               HorizontalOptions="Fill"
                                               VerticalOptions="Fill"
                                               SemanticProperties.Description="{Binding Title}" />
                                    </Border>
                                    <Border HorizontalOptions="End"
                                            VerticalOptions="Start"
                                            Margin="8"
                                            Padding="0"
                                            WidthRequest="32"
                                            HeightRequest="32"
                                            StrokeThickness="0"
                                            StrokeShape="RoundRectangle 16"
                                            Background="#236B5A"
                                            IsVisible="{Binding Source={x:Reference Page}, Path=ViewModel.IsSelectionMode, x:DataType=views:LibraryPage}">
                                        <Label Text="✓"
                                               TextColor="White"
                                               FontSize="20"
                                               HorizontalTextAlignment="Center"
                                               VerticalTextAlignment="Center"
                                               IsVisible="{Binding IsSelected}" />
                                    </Border>
                                </Grid>
                                <Label Grid.Row="1"
                                       Text="{Binding Title}"
                                       FontAttributes="Bold"
                                       FontSize="16"
                                       TextColor="#F2F5F8"
                                       LineBreakMode="TailTruncation"
                                       MaxLines="2" />
                                <Label Grid.Row="2"
                                       Text="{Binding Author}"
                                       TextColor="#9AA5B5"
                                       FontSize="13"
                                       LineBreakMode="TailTruncation"
                                       MaxLines="1" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                <CollectionView.EmptyView>
                    <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="12">
                        <Label Text="Your library is empty"
                               FontSize="20"
                               TextColor="#F2F5F8"
                               HorizontalOptions="Center"
                               IsVisible="{Binding IsLibraryEmpty}" />
                        <Label Text="Added books will appear here."
                               TextColor="#9AA5B5"
                               HorizontalOptions="Center"
                               IsVisible="{Binding IsLibraryEmpty}" />
                        <Label Text="No books match your search"
                               FontSize="20"
                               TextColor="#F2F5F8"
                               HorizontalOptions="Center"
                               IsVisible="{Binding IsSearchEmpty}" />
                        <Label Text="Try a different title or author."
                               TextColor="#9AA5B5"
                               HorizontalOptions="Center"
                               IsVisible="{Binding IsSearchEmpty}" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
            </CollectionView>
        </RefreshView>

        <!-- Import progress overlay -->
        <Grid Grid.RowSpan="4"
              Background="#CC000000"
              IsVisible="{Binding IsImporting}"
              ZIndex="10"
              Padding="24">
            <Border Background="#1A1E24"
                    Stroke="#2A3038"
                    StrokeThickness="1"
                    StrokeShape="RoundRectangle 20"
                    Padding="24"
                    MaximumWidthRequest="520"
                    HorizontalOptions="Fill"
                    VerticalOptions="Center">
                <VerticalStackLayout Spacing="16">
                    <Grid ColumnDefinitions="*,84" ColumnSpacing="16">
                        <VerticalStackLayout Spacing="4"
                                             HorizontalOptions="Fill">
                            <Label Text="Adding books"
                                   FontSize="24"
                                   FontAttributes="Bold"
                                   TextColor="#F2F5F8" />
                            <Label Text="{Binding ImportStage}"
                                   TextColor="#9AA5B5"
                                   FontSize="15"
                                   LineBreakMode="TailTruncation"
                                   MaxLines="1"
                                   HeightRequest="24" />
                        </VerticalStackLayout>
                        <Label Grid.Column="1"
                               Text="{Binding ImportProgressPercent}"
                               FontSize="28"
                               FontAttributes="Bold"
                               TextColor="#66E0C1"
                               HorizontalTextAlignment="End"
                               VerticalOptions="Center" />
                    </Grid>
                    <ProgressBar Progress="{Binding ImportProgress}"
                                 ProgressColor="#236B5A"
                                 Background="#2A3038"
                                 HeightRequest="10" />
                    <Label Text="{Binding ImportCurrentItem}"
                           FontSize="14"
                           TextColor="#F2F5F8"
                           LineBreakMode="TailTruncation"
                           MaxLines="1"
                           HeightRequest="20" />
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                        <Label Text="{Binding ImportProgressSummary}"
                               TextColor="#9AA5B5"
                               VerticalOptions="Center" />
                        <Button Grid.Column="1"
                                Text="Cancel"
                                Command="{Binding CancelImportCommand}"
                                Background="#A83A3A"
                                TextColor="White"
                                CornerRadius="10"
                                Padding="18,10" />
                    </Grid>
                </VerticalStackLayout>
            </Border>
        </Grid>
    </Grid>
</ContentPage>
